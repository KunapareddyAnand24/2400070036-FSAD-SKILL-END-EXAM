<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- ===== DATABASE CONNECTION SETTINGS ===== -->
        <!-- JDBC Driver -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>

        <!-- Database URL — change host/port if needed -->
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/fsadendexam?createDatabaseIfNotExist=true&amp;useSSL=false&amp;serverTimezone=UTC
        </property>

        <!-- Credentials — update as per your MySQL setup -->
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">root</property>

        <!-- ===== HIBERNATE DIALECT ===== -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- ===== SESSION / POOL SETTINGS ===== -->
        <property name="hibernate.connection.pool_size">5</property>
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- ===== DDL / SCHEMA ===== -->
        <!-- update  → keeps existing data, alters schema to match entities  -->
        <!-- create  → drops + recreates tables on every start               -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- ===== LOGGING ===== -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- ===== MAPPED ENTITY CLASSES ===== -->
        <mapping class="com.klef.fsad.exam.Hospital"/>

    </session-factory>
</hibernate-configuration>
